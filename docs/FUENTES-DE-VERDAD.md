---
title: "Fuentes de verdad del repo — por tema"
date: 2026-05-19
author: "Claude (Anthropic - Claude Opus 4.7 1M)"
requested-by: "Gustavo Viollaz (@gviollaz)"
status: vivo
tipo: indice
---

# Fuentes de verdad — qué doc/módulo leer para cada tema

> **Por qué existe este archivo.** El repo tiene 49 docs creados por sesiones
> Claude descoordinadas (mayo 2026). Muchos temas tienen 2-3 docs rivales sin
> indicación clara de cuál es canónico. Esta tabla resuelve eso: una fila por
> tema, un único doc canónico vigente, los demás marcados como superados.
>
> **Regla de actualización**: cualquier sesión que crea o supera un doc tiene
> que actualizar esta tabla en el mismo commit. Si no, no es una sesión válida.

## Tabla canónica

| Tema | Doc/módulo canónico (vigente) | Superados / no usar como guía |
|---|---|---|
| **Arquitectura general 3 placas** | `docs/ARQUITECTURA-3-PLACAS-2026.md` (la sección "Mapa de flujo de datos" tiene WorldSnapshot v1 24 B — ver tema WorldSnapshot abajo) | — |
| **Mapa de datos end-to-end (qué corre + qué mensaje va a quién/cómo)** | **`docs/MAPA-DE-DATOS.md`** (índice "start-here": cada mensaje con tipo/tamaño/transporte/pin/freq + quién lo llena/consume + link al contrato canónico) | — |
| **FSM táctica CENTRAL (la que CORRE)** | `src/central/strategy.cpp` (código vivo, llamado por `main_central.cpp:113`) + `src/shared/strategy_transitions.{h,cpp}` como caracterización pura con 35 tests | `_archive/src/shared/strategy_core.*` (FSM alternativa nunca integrada). `docs/superpowers/specs/2026-05-18-central-strategy-core-design.md` (diseño de la alternativa archivada — útil como referencia post-Incheon, NO refleja lo que corre hoy) |
| **FSM táctica CENTRAL (docs)** | `docs/firmware/FIRMWARE-PLACA-CENTRAL.md §8` (sincronizada con `strategy.cpp` real) | Diferentes sistemas de nombres en otros docs (SEEK/DRIVE vs SEARCH/POSITION) son la **misma máquina con etiquetas distintas**, no FSM rivales |
| **Behind-the-ball** | `src/shared/behind_ball.{h,cpp}` + 16 tests. Llamado por `strategy.cpp:230` (estado POSITION). Versión RELATIVA (sin pose absoluta). | El pseudocódigo de `FIRMWARE-PLACA-CENTRAL.md §8.4/§8.5` con `OPP_GOAL_X/Y` es Nivel 3 futuro (requiere EKF) — ya marcado en el doc |
| **Fusión cámaras (TOP)** | `src/shared/cameras_fusion.{h,cpp}` + `src/top/cameras_runtime.{h,cpp}` + 16 tests. Llamado por `main_top.cpp`. | — (sin doc rival) |
| **WorldSnapshot (struct compartido)** | `docs/firmware/CONTRATO-DATOS-CENTRAL.md` + `CONTRATO-DATOS-TOP.md §3` (**v3 = 31 B**: agrega `heading_valid` (flag bit4) + ángulo/distancia del arco propio; `static_assert(sizeof==31)` en `types.h`) | "v2 = 27 B" y "24 B" (mapa de flujo de `ARQUITECTURA-3-PLACAS-2026.md`) — **superados** por v3 (2026-06-03) |
| **DOWN — lectura de línea (la que CORRE)** | **Dos cadenas en paralelo (deuda viva, no archivar antes de Incheon):** (1) `src/down/line_ring.{h,cpp}` llamado por `main_down.cpp` para lectura cruda 1 kHz; (2) `src/down/down_model + line_geometry + line_tracker + line_calib + surface_monitor + down_encode` llamados por `comm_central.cpp` para armar `LineStatusV2` que va al CENTRAL. **Robustez (2026-05-29):** `dm_update` rechaza saturación "todo blanco" vía `lf_all_white()` y persiste/carga calib en EEPROM (`comm_central_load_persisted_calib`). | `docs/firmware/FIRMWARE-PLACA-ABAJO.md` describe solo (1) — desactualizado, banner agregado |
| **DOWN — pinout Teensy↔CD4051↔sensores** | **`hardware/electronics/2026-05-19-pinout-down-extraido-schematic.md`** + copia en el pack `down-board-pack/01-pinout-y-posiciones.md`. **VALIDADO EMPÍRICAMENTE 2026-05-24** en banco (Gustavo + Claude, sesión ejecución directa): pinout aplicado al firmware (config_down.h + line_ring.cpp), verdict del diag_capture = 0 muertos, los 32 sensores responden. La arquitectura "cada mux tiene 3 SEL propios" quedó confirmada. Cierre formal con multímetro (TASK-026) baja de P0 a P2. Ver journal `2026-05-24-hardware-up-down-anillo-linea.md`. | `src/down/config_down.h` ANTERIOR (`PIN_MUX_SEL_A=2,B=3,C=4,INH=5..8`, `PIN_MUX_OUT={A0,A1,A2,A3}`) — reemplazado. Sección DOWN de `mapa-pines-placas-nuevas.md` líneas 130-134 ("A/B/C compartidas") — SUPERADO. |
| **DOWN — pack autocontenido para programar (punto de entrada IA)** | **`hardware/electronics/down-board-pack/`** — todo lo necesario para entender, programar, testear y diagnosticar la placa DOWN en un solo lugar. 47 archivos, 2.4 MB. Contiene: 4 docs canónicos curados (`01-pinout` + `02-funcionalidad` + `03-contrato-datos` + `04-protocolo-comunicaciones`) + snapshot del firmware vivo (`firmware/down/` + módulos `firmware/shared/`) + 8 tests host-native + diag de hardware + scripts + ground-truth (SCH/PCB JSONs + PDF + BOM). El `README.md` tiene un índice "pregunta → doc". **Regla**: si el pack contradice al repo vivo, gana el repo vivo (es snapshot del 2026-05-24). | Otros docs que tocan DOWN tangencialmente (journals, plans superpowers, research in-progress, team-tasks) — quedan en sus carpetas originales como historia/gestión, no se duplican al pack. |
| **CENTRAL — pack autocontenido para programar (punto de entrada IA)** | **`hardware/electronics/central-board-pack/`** — equivalente al pack DOWN pero para la placa CENTRAL (Zircon Rev v15 + Teensy 4.1). 39 archivos. Contiene: 5 docs canónicos curados (`01-pinout-y-hardware` + `02-funcionalidad` 3 capas FSM/PIDs/motores + `03-contrato-datos` WorldSnapshot v2 + `04-protocolo-comunicaciones` + `05-arquitectura-3-placas`) + snapshot del firmware vivo de `src/central/` (14 archivos) + módulos `src/shared/` usados por CENTRAL (strategy_transitions, behind_ball, pids, kinematics, motion_target, proto, types) + 7 tests host-native (test_strategy_transitions 35 + test_behind_ball 16 + test_pids 17 + test_kinematics 11 + central_contract/trajectory/motion). Documenta los 2 robots (ROBOT1 arquero + ROBOT2 delantero) con sus pinouts distintos. **NO incluye ground-truth** (no hay schematic JSON del Zircon disponible, solo `Zircon.pdf` que queda fuera por binario pesado). **Conflicto pines 7/8 RESUELTO 2026-05-31**: se movieron los UART (DOWN→CENTRAL a Serial1 0/1, TOP→CENTRAL a Serial7 28/29); los pines 7/8 quedan solo para el driver del motor 2 (U17). | Igual que DOWN: journals, tasks, plans no se duplican al pack. `Zircon.pdf` queda fuera. `software/libraries/zirconLib/` queda fuera (legacy 2025). |
| **TOP — pack autocontenido para programar (punto de entrada IA)** | **`hardware/electronics/top-board-pack/`** — equivalente al pack DOWN/CENTRAL pero para la placa TOP (Teensy 4.0 master + PCB custom "Roboliga2026 TOP"). ~36 archivos. Contiene: 6 docs canónicos curados (`01-pinout-y-hardware` + `02-funcionalidad` + `03-contrato-datos-top` + `04-contrato-datos-camaras` + `05-protocolo-comunicaciones` + `06-arquitectura-3-placas`) + snapshot del firmware vivo de `src/top/` + módulos `src/shared/` + tests host-native + ground-truth. **Actualización 2026-05-30/31**: (1) los 4 ToF VL53L7CX ya NO son stub — enumeran en el bus `Wire` con LP por bodge (pines **{9,10,11,12}** → 0x2A..0x2D, confirmado en banco); el **bus `Wire2` (24/25)** del TOP quedó **liberado** (corrección 2026-06-09: los pines 24/25 del Teensy 4.0 son `Wire2`/LPI2C4, antes mal llamado "Wire1"; es el bus del 2º BNO de ROBOT2 — ver TASK-207). (2) **swap 2026-05-31 (TASK-204)**: la **cámara trasera quedó soldada en Serial5 (pin 21)** (confirmado en banco), así que el UART **TOP→CENTRAL se movió a Serial7 (28/29)** (`comm_central.cpp` → Serial7; `cameras_runtime.cpp` → trasera Serial5); además el HC-SR04 quedó en pines 4/3. (3) **pin 10 colisiona** entre LP ToF[1] y dipswitch de rol → reubicar. | Igual que DOWN/CENTRAL: journals, tasks, plans no se duplican al pack. `mapa-pines-placas-nuevas.md` y `2026-05-17-placa-top-analisis-gerbers.md` quedan en sus carpetas como historia. |
| **Convención de izquierda/derecha + ejes del robot (primera persona)** | **`docs/CONVENCION-EJES-ROBOT.md`** — CANÓNICA. +X=derecha, +Y=frente, izq/der en primera persona (parado EN el robot mirando al frente). Mapeo ToF (TOF2=derecha, TOF3=izquierda) + sentido de giro CW/CCW + temas a verificar en HW. | Comentarios contradictorios YA corregidos: `cameras_fusion.h` (decía +90=izq), `localization.h` (decía [2]=izq,[3]=der). Si algún archivo contradice el doc canónico, el archivo está mal. |
| **Convención de ejes de la cancha (X/Y/θ del robot)** | **`docs/CONVENCION-EJES-ROBOT.md` §2** (CANÓNICA, corregida 2026-06-03 contra el field spec oficial RCJ 2026) + `src/top/pinout_common.h` (`FIELD_HEIGHT_MM=2430` = eje **Y** arco-a-arco/largo; `FIELD_WIDTH_MM=1820` = eje **X** lateral/corto). +Y va al arco rival = lado LARGO. | `docs/superpowers/specs/2026-05-25-localization-sprint1-trilateration-design.md` §4.1 (decía +X=largo/+Y=corto — **INVERTIDO**, banner de corrección 2026-06-03). `docs/firmware/FIRMWARE-PLACA-ARRIBA.md` §6.4 (dimensiones X=1820/Y=2430 correctas, pero usa heading legacy θ=90°=+Y, no la convención canónica heading-0=+Y) |
| **CENTRAL — motores: índice↔driver↔pines + sentido + posición + ángulos + piso** | `src/central/config_central.h` (pines + `MOTOR_INVERT` + `WHEEL_ANGLES_DEG` + `MOTOR_MIN_PWM[3]`, todos POR robot en el `#if`) + `src/central/motors_zircon.cpp`. ROBOT1 **DEFINIDO en banco** (Gustavo 2026-06-08 + María/Elías 2026-06-01): posición física **M1=delantera-IZQUIERDA (U5 2/5/3) · M2=delantera-DERECHA (U17 8/7/6, INVERTIDO HW) · M3=trasera (U7 11/12/4)**; los 3 giran HORARIO desde el centro → `MOTOR_INVERT={+1,-1,+1}`, `WHEEL_ANGLES_DEG={330,210,90}`, piso de PWM **por rueda** `MOTOR_MIN_PWM[3]={70,70,42}` (delanteras oblicuas necesitan más piso que la trasera paralela). | `{+1,+1,+1}` sin inversión (vieja "Resultados de calibración" de `DIAG-CENTRAL-MOTORS.md`) — **superado**. `WHEEL_ANGLES_DEG={60,-60,180}` ("TENTATIVO / da círculos") — **superado** por `{330,210,90}` (2026-06-08: el viejo estaba en eje +Y y la fórmula usa +X). Piso único escalar `MOTOR_MIN_PWM=N` y `MOTOR_GAIN` — **superados** por el array por rueda. ⚠️ ROBOT2/delantero sin validar (pines rotados). Pendiente banco ROBOT1: SOLO tuneo fino del lateral (que no rote) + confirmar sentido. |
| **CENTRAL — control de movimiento de bajo nivel (impulso/freno/rampas/PWM por movimiento)** | `docs/firmware/MOTION-CONTROL-ACTUAL.md` (estrategia 2026: cinemática + piso por rueda; GAP honesto vs histórico con P1/P2) + `docs/firmware/MOTION-CONTROL-HISTORICO.md` (valores reales 2025: impulso 150×70 ms para giro, freno anticipado de la trasera 60 ms auto-calibrado por gyro, plugging M1=250/M2=170 ×200–300 ms). Auto-generados del análisis de `software/_deprecated-2025/` + `software/staging/shared/test-*` (2026-06-08). **+ `docs/firmware/MOTION-CONTROL-PLAN-2026.md`** (plan de portado en 4 capas, GATEADO) + módulos PUROS host-testeados `src/shared/{gk_motion_speed,motor_kickstart,motor_brake}.{h,cpp}` (Capas 1-2; gateados, default OFF → binario de competencia idéntico; Capa 3 cascada pendiente). | El firmware 2026 HOY solo tiene piso de deadzone por rueda — NO porta impulso/freno-reversa/rampas (módulos escritos pero NO cableados; pendiente de banco). `software/_deprecated-2025/` y `software/staging/` están CONGELADOS (no flashear). |
| **DOWN — contrato de datos** | `docs/firmware/CONTRATO-DATOS-DOWN.md` (LineStatusV2) | — |
| **TOP — contrato de datos** | `docs/firmware/CONTRATO-DATOS-TOP.md` | — |
| **TOP — cámaras (protocolo)** | `docs/firmware/CONTRATO-DATOS-CAMARAS.md` | — |
| **Cámaras (OpenMV) — script que va EN la cámara (PRODUCCIÓN)** | **`hardware/electronics/camaras-openmv/main.py`** — el que se copia como `main.py` en las 2 cámaras del ROBOT1. Detección que ANDA (LED indicador + colores LAB calibrados + recorte inferior ROI + homografía de Elías @VGA) + comunicación **v2 con CRC8+END**. README al lado; `main-comunicacion-vieja.py` = referencia v1 (NO flashear). | ⛔ `cameraFront-pack/.../cam-frontal-n6.py` y `cameraBack-pack/.../cam-trasera-n6.py` — reescritura SIN LED/colores/recorte → NO detecta → **DEPRECADOS** (banner 2026-06-08). NO flashear esos. |
| **Cámaras (OpenMV N6) — calibración de COLOR (LAB/exposición)** | `docs/firmware/CALIBRACION-VISION-N6.md` (+ kit `calib-lab-n6.py`) | — |
| **Cámaras (OpenMV N6) — calibración de HOMOGRAFÍA/XY** | `docs/firmware/CALIBRACION-HOMOGRAFIA-XY-N6.md` (lona con grilla + `calib-homografia-n6.py` en cámara + `vision-optimization-pack/tools/solve_homografia.py` en PC) | El "Paso 4 — Homografía" (stub de 4 puntos a ojo) de `CALIBRACION-VISION-N6.md` y el §3 de los `04-calibracion-lab-y-homografia.md` de los packs — **superados** (ahora apuntan a este doc). |
| **Cámara FRONTAL (OpenMV) — pack autocontenido** | **`hardware/electronics/cameraFront-pack/`** — pack equivalente a los de placas pero para la cámara OpenMV frontal. 16 archivos, 152 KB. Contiene 4 docs curados (`01-hardware-y-conexion` + `02-funcionalidad` + `03-protocolo-comunicacion` + `04-calibracion-lab-y-homografia`) + snapshot del firmware OpenMV genérico actual + **template objetivo `target-cam-frontal-template.py` con bugs P0 corregidos** + parser del lado Teensy + tests de fusión. Para reemplazar el script genérico actual (`software/vision/enviar coordenadas 2 arcos y pelota`) por uno específico calibrado para esta cámara (TASK-022). | Pack hermano: `cameraBack-pack/`. El skill `openmv-vision-tuning` cubre el workflow general (no se duplica en el pack). |
| **Cámara TRASERA (OpenMV) — pack autocontenido** | **`hardware/electronics/cameraBack-pack/`** — pack equivalente al frontal pero para la cámara trasera. 16 archivos, 156 KB. Misma estructura, con énfasis especial en la **rotación 180°** que el TOP aplica a sus coordenadas (`cameras_fusion.cpp:25-29` con `cam_id=1`). Documenta por qué la cámara trasera NO debe rotar sus coordenadas internamente — la rotación es responsabilidad del TOP. Diferencia clave con la frontal: distinto Serial del Teensy (**Serial5** vs Serial3 — la trasera quedó soldada en Serial5, confirmado en banco 2026-05-31; ver fila TOP), distinto conector (U9 vs U8), distinto cam_id (1 vs 0), H_MATRIX independiente, HMIRROR/VFLIP probablemente distintos por montaje espejado. | Pack hermano: `cameraFront-pack/`. |
| **Comunicaciones entre placas (diseño)** | `docs/decisions/2026-05-18-diseno-comunicaciones-robusto-definitivo.md` (declara fuente única, reemplaza al protocolo-comunicaciones-entre-placas.md del mismo día) | `docs/decisions/2026-05-18-protocolo-comunicaciones-entre-placas.md` (base conceptual superada). `docs/decisions/2026-05-18-verificacion-protocolo-comunicaciones.md` (insumo de la verificación, no decisión) |
| **Comunicación inter-robot (SuperTeam)** | `docs/decisions/2026-05-17-comunicacion-inter-robot-superteam.md` (decisión VIGENTE: NO implementar para Incheon, Opción A). La **arquitectura concreta de la Opción B** (gateway ESP32-S3 en el bus CAN, gate de modo árbitro) vive en `docs/decisions/2026-06-03-bus-can-general-y-flasheo-por-can.md` §7-bis (forward-looking, NO cambia la decisión) | — |
| **Bus general CAN + flasheo por CAN (PROPUESTA, post-Incheon)** | `docs/decisions/2026-06-03-bus-can-general-y-flasheo-por-can.md` — propuesta de migrar el transporte inter-placa de UART punto-a-punto a un **bus CAN único** (3 Teensy + 4 OpenMV + bridge ESP32-S3, transceiver SN65HVD230, JST-GH) y de **flashear firmware por el bus** (FlasherX, integridad CRC en 3 capas, gate de mantenimiento). **§7-bis** analiza la extensión inter-robot (SuperTeam) por el mismo gateway. **Apéndice A**: esquema de interfaz CAN por Teensy (SN65HVD230) + pines CAN1/2/3 + BOM/costo + sketch PoC Fase 0. **Status: propuesta, NO validada en banco.** | **NO reemplaza** el diseño VIVO de comunicaciones UART (`2026-05-18-diseno-comunicaciones-robusto-definitivo.md`), que sigue siendo el transporte canónico que CORRE. Las **capas fail-safe 0–6** de ese doc se **preservan**; CAN solo cambia las capas 1 (física) y 2 (framing). |
| **Firmware placa COMM (ESP32-C6) — flash** | `hardware/electronics/comm-board/2026-05-17-procedimiento-flash-firmware-c6.md` + `2026-05-17-placa-comm-componentes-y-circuito.md` (branch `esp32-c6`, pinout 6/7/18/9/20/19, BLE `RCJs-m_<MAC>`) | `journal/2026-05-15-firmware-comm-c6-flash-procedure.md` (branch `master`/C5, pinout 2/3/10/7/9/8 — INCORRECTO, ya tiene banner) |
| **Setup entorno PlatformIO + Avast** | `docs/firmware/SETUP-ENTORNO-BUILD-WINDOWS.md` (con callout Avast del 18-may) + `team-tasks/TASK-025` (excepción Avast por máquina) | — |
| **PIDs (heading, lateral, distancia)** | `src/shared/pids.{h,cpp}` + 17 tests. Llamado desde `strategy.cpp`. | — |
| **Kinemática inversa omni-3** | `src/shared/kinematics.{h,cpp}` + 11 tests. Llamado desde `motors_zircon.cpp`. | — |
| **CENTRAL — diag de motores en banco** | [`docs/firmware/DIAG-CENTRAL-MOTORS.md`](firmware/DIAG-CENTRAL-MOTORS.md) + sketch [`src/diag/diag_central_motors.cpp`](../software/teensy/Soccer%202026/src/diag/diag_central_motors.cpp) + env `[env:diag_central_motors]` en `platformio.ini`. Test individual de los 3 H-bridges del Zircon Rev v15 con onda PWM controlada por botón (pin 9). Agnóstico al rol — usa pines hardcodeados, no `config_central.h`. Sirve para mapear "motor N firmware → rueda física". (El conflicto 7/8 ya se resolvió 2026-05-31 moviendo los UART a Serial1/Serial7; los pines 7/8 quedaron para el motor 2.) | — |
| **CENTRAL — patrulla del arquero gatillada por el árbitro** | [`docs/firmware/DIAG-CENTRAL-ARBITRO-STRAFE.md`](firmware/DIAG-CENTRAL-ARBITRO-STRAFE.md) + sketch [`src/diag/diag_central_arbitro_strafe.cpp`](../software/teensy/Soccer%202026/src/diag/diag_central_arbitro_strafe.cpp) + envs `[env:diag_central_arbitro_strafe_robot1/2]`. Espera el START del árbitro (`world_model_match_running()` del WorldSnapshot del TOP, Serial7), patrulla lateral 30 cm IZQ→pausa 3 s→DER→pausa 3 s, STOP→frena, START→retoma. Override de banco por Serial (`s`/`x`) para probar sin COMM/TOP. OPEN-LOOP + mismos caveats de cinemática que el strafe por botón. | — |
| **CENTRAL — verificar recepción completa de TOP + DOWN** | [`docs/firmware/DIAG-CENTRAL-RX-ALL.md`](firmware/DIAG-CENTRAL-RX-ALL.md) + sketch [`src/diag/diag_central_rx_all.cpp`](../software/teensy/Soccer%202026/src/diag/diag_central_rx_all.cpp) + env `[env:diag_central_rx_all]`. Escucha DOWN (Serial1/pin 0) **y** TOP (Serial7/pin 28), decodifica los 4 mensajes (LineStatusV2 0x10 + Pose2D 0x11 + Velocity2D 0x12 de DOWN; WorldSnapshot 0x60 de TOP) campo por campo, y da un **veredicto de completitud** (`[OK]/[STALE]/[FALTA]` + Hz por mensaje + PASS/FAIL global + LED). Responde "¿la CENTRAL recibe TODO de TOP+DOWN?". (Para ver SOLO la línea de DOWN con más detalle: `DIAG-CENTRAL-COMM-DOWN.md`.) | — |
| **CENTRAL — diag de avance recto con PID heading** | [`docs/firmware/DIAG-CENTRAL-DRIVE.md`](firmware/DIAG-CENTRAL-DRIVE.md) + sketch [`src/diag/diag_central_drive_straight.cpp`](../software/teensy/Soccer%202026/src/diag/diag_central_drive_straight.cpp) + envs `[env:diag_central_drive_robot1]` y `[env:diag_central_drive_robot2]`. Valida end-to-end la cadena `WorldSnapshot (Serial1 TOP) → world_model → HeadingPID → kinematics inversa omni-3 → motors_zircon`. Botón pin 9 controla: FORWARD 3s → PAUSED 1s → REVERSE 3s → DONE. Sirve para tunear ganancias del HeadingPID y confirmar cinemática del robot armado. **NO hace PID diferencial OTOS** (CENTRAL no recibe las 2 lecturas crudas; requiere contrato v3 o bypass DOWN→CENTRAL, ambos post-Incheon). | — |

## Deudas conocidas (no se resuelven antes de Incheon)

1. **DOWN lectura dual** (`line_ring` vivo + cadena nueva `down_model` viva en
   paralelo, posiblemente leyendo sensores 2 veces). Funciona ambos por
   separado. Decisión binaria pendiente post-Incheon: integrar todo en la
   cadena nueva o documentar formalmente la separación. → revisar en
   journal post-Nacional (noviembre 2026).
2. **Conectar `strategy.cpp` → `strategy_transitions`** (cerrar la red de
   testing de la FSM viva). Diseño en `journal/2026-05-15-analisis-firmware-y-fsm-testeable.md`
   "Conectar strategy.cpp" — pendiente ejecutar en post-Incheon (riesgo:
   tocar el cerebro).
3. ~~**`cameras_runtime` no llena `ball_vx/vy`**~~ → **RESUELTO (2026-06-03).** El
   módulo puro `ball_velocity` deriva la velocidad de la pelota y `cameras_runtime`
   la cablea (`cameras_get_ball_vx/vy_mm_s`); `build_snapshot` la mete al snapshot.
   El arquero ya la usa para anticipar (`ball_predict`). Fallback exacto con pelota quieta.
4. **TOP — ToF: ✅ RESUELTO por bodge de Enzo (2026-05-30).** El forense del
   2026-05-25 había concluido que los XSHUT/LP de los 4 ToF NO estaban ruteados
   en TOP rev 1.0 (máximo 2 ToFs sin rework). **Eso quedó SUPERADO**: Enzo
   cableó por bodge la pata LP de cada ToF a un pin del Teensy y movió los **4
   ToF al bus `Wire` (18/19)**. Banco 2026-05-30 (`diag_top_tof_census`, tras
   power-cycle): los **4 LP funcionan** → pines **{9,10,11,12} activo-alto**,
   enumeran a **0x2A/0x2B/0x2C/0x2D**. Esto liberó el bus `Wire2` (24/25) del TOP
   (corrección 2026-06-09: el bus de 24/25 es `Wire2`/LPI2C4, no `Wire1`; es donde va
   el 2º BNO — TASK-207) y habilita localización 2D por trilateración. Pines/direcciones reales
   ya en `pinout_robot1.h`/`pinout_robot2.h` (`PIN_TOF_XSHUT={9,10,11,12}`,
   `NUM_TOF_ACTIVE=4`). **Lección**: las direcciones I²C de los VL53L7CX
   persisten con 3V3 → power-cycle obligatorio al enumerar (no reset).
   Pendiente (firmware, NO bloqueante de hardware): confirmar ranging con
   `diag_top_tof_quad_live` + mapear posición física + extender `sensors_tof.cpp`
   (HAL Sprint B). Conflicto colateral: pin 10 = LP ToF y dipswitch de rol a la
   vez → reubicar rol. Ver `journal/2026-05-30-top-tof-4-en-bus-unico-enumeracion-ok.md`.
   (Plan de escalado: 6 ToF = 4 fijos + 2 móviles para pelota, ver `pinout_common.h`.)

## Mapa de docs superados (con banner)

Estos docs tienen banner explícito que apunta acá:

- `journal/2026-05-15-firmware-comm-c6-flash-procedure.md` → ver procedure del 17-may
- `docs/firmware/FIRMWARE-PLACA-ABAJO.md` → ver `CONTRATO-DATOS-DOWN.md` + this file
- `docs/ARQUITECTURA-3-PLACAS-2026.md` Mapa de flujo → ver `CONTRATO-DATOS-CENTRAL.md` para tamaño real de WorldSnapshot
- `docs/superpowers/specs/2026-05-18-central-strategy-core-design.md` → módulo archivado en `_archive/`, ver este archivo

## Cómo mantener esta tabla viva

- Cualquier PR que cree un doc nuevo de tema técnico → agregar fila acá.
- Cualquier PR que supere un doc existente → marcar superado acá + banner en el doc viejo.
- Sesión Claude que toca el repo: primera lectura obligatoria + `docs/ESTADO-ACTUAL.md`.
