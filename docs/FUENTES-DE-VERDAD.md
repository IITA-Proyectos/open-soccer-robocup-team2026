---
title: "Fuentes de verdad del repo — por tema"
date: 2026-05-19
author: "Claude (Anthropic - Claude Opus 4.7 1M)"
requested-by: "Gustavo Viollaz (@gviollaz)"
status: vivo
tipo: indice
---

# Fuentes de verdad — qué doc/módulo leer para cada tema

> **Por qué existe este archivo.** El repo tiene 49 docs creados por sesiones
> Claude descoordinadas (mayo 2026). Muchos temas tienen 2-3 docs rivales sin
> indicación clara de cuál es canónico. Esta tabla resuelve eso: una fila por
> tema, un único doc canónico vigente, los demás marcados como superados.
>
> **Regla de actualización**: cualquier sesión que crea o supera un doc tiene
> que actualizar esta tabla en el mismo commit. Si no, no es una sesión válida.

## Tabla canónica

| Tema | Doc/módulo canónico (vigente) | Superados / no usar como guía |
|---|---|---|
| **⚑ Modo de trabajo del repo (competencia vs aprendizaje)** | **`docs/MODO-APRENDIZAJE.md`** (2026-07-23) — Incheon TERMINÓ (jun 30–jul 6 2026). Define quién implementa (Gustavo, con la placa en la sesión), la escala P0/P1/P2 nueva (P0 = bloquea el aprendizaje, no "bloqueante para Incheon"), el rango TASK-400+ y las 3 líneas de investigación abiertas. | El frame de competencia de `CLAUDE.md` (P0 = bloqueante para Incheon) — **histórico**. Los "no tocar / antes de Incheon / competencia byte-idéntica" de `ESTADO-ACTUAL.md` y `BACKLOG-INCHEON.md` — **historia técnica válida, instrucción vencida**. |
| **¿Qué env flasheo hoy? (vigentes vs lista negra)** | **`docs/pruebas-banco/QUE-FLASHEO-HOY.md`** (tabla robot×placa + lista negra de envs trampa + teclas; se actualiza en el mismo commit que cualquier cambio de env recomendado) · companion día-de-cancha que la replica y le suma app/IMU ZERO/gotchas + marca conflictos: `docs/COMANDOS-COMPETENCIA.md` | Los banners sueltos de ESTADO-ACTUAL y el header de `platformio.ini` (congelado en mayo) — siguen siendo historia útil pero la tabla manda |
| **Backlog priorizado Incheon (temas pendientes consolidados)** | **`docs/BACKLOG-INCHEON.md`** (auditoría 2026-06-11: P0/P1/P2 deduplicados y verificados contra el código; vivo) | Listas parciales en MEJORAS-PENDIENTES / MEJORAS-ANALISIS-UNO-POR-UNO / team-tasks README (siguen siendo las fuentes de detalle por ítem; el backlog las consolida) |
| **Arquitectura general 3 placas** | `docs/ARQUITECTURA-3-PLACAS-2026.md` (la sección "Mapa de flujo de datos" tiene WorldSnapshot v1 24 B — ver tema WorldSnapshot abajo) | — |
| **Mapa de datos end-to-end (qué corre + qué mensaje va a quién/cómo)** | **`docs/MAPA-DE-DATOS.md`** (índice "start-here": cada mensaje con tipo/tamaño/transporte/pin/freq + quién lo llena/consume + link al contrato canónico) | — |
| **FSM táctica CENTRAL (la que CORRE)** | `src/central/strategy.cpp` (código vivo, llamado por `main_central.cpp:113`) + `src/shared/strategy_transitions.{h,cpp}` como caracterización pura con 35 tests | `_archive/src/shared/strategy_core.*` (FSM alternativa nunca integrada). `docs/superpowers/specs/2026-05-18-central-strategy-core-design.md` (diseño de la alternativa archivada — útil como referencia post-Incheon, NO refleja lo que corre hoy) |
| **FSM táctica CENTRAL (docs)** | `docs/firmware/FIRMWARE-PLACA-CENTRAL.md §8` (sincronizada con `strategy.cpp` real) | Diferentes sistemas de nombres en otros docs (SEEK/DRIVE vs SEARCH/POSITION) son la **misma máquina con etiquetas distintas**, no FSM rivales |
| **FSM táctica CENTRAL (guía de lectura de alto nivel)** | **`docs/firmware/STRATEGY-CPP-COMO-FUNCIONA.md`** (mapa mental para entender `strategy.cpp` antes de abrirlo: entra→decide→sale, qué es una FSM, los 2 cerebros delantero/arquero, por qué el archivo es largo, orden de lectura recomendado; voz de estudiante) | NO reemplaza al §8 (spec detallado) ni al código vivo — es la rampa de entrada que apunta a ambos |
| **TOP — arquitectura del lazo sensorial no-bloqueante (pizarra + RX-IRQ + emisor por timer)** | **`docs/firmware/ARQUITECTURA-SENSORIAL-TOP-NO-BLOQUEANTE.md`** (2026-06-15, diseño + andamiaje gateado off-by-default: el cuello NO es el RX (el core ya drena por IRQ) sino que `build_snapshot` comparte el superloop con lecturas I²C/`pulseIn` bloqueantes; pizarra slot+seqlock+frescura desacopla el snapshot de 100 Hz. Corrige 2 premisas del equipo: DMA en RX de cámaras es redundante (parseo en RX-IRQ da el 90 %); el "DMA TX" se hace con la cola TX interrupt-driven del core, no `DMAChannel`. Límite físico: BNO+4 ToF en un solo `Wire` → paralelismo cero entre ellos. ⚠️ Review adversarial: la barrera del seqlock debe ser `__DMB()` con clobber `:::"memory"` (NO el `dsb` pelado del watchdog); el seqlock es correcto por **single-writer + lector wait-free**, NO por "ISR no interrumpe a ISR" (falso en el M7 por preempción anidada del NVIC). Módulo puro a andamiar = `sensor_slot.h` (mismo nombre que CENTRAL/DOWN; el `blackboard_slot.h` del cuerpo del doc es alias desalineado). NO reescribe `main_top.cpp` vivo) | — (diseño; `main_top.cpp::build_snapshot` sigue siendo lo que CORRE) |
| **DOWN — arquitectura del lazo RT (lectura veloz del anillo + detección temprana confiable + difusión sin demoras)** | **`docs/firmware/ARQUITECTURA-LAZO-DOWN-RT.md`** (2026-06-15, diseño + andamiaje gateado off-by-default: 4 capas (adquisición→pizarra→cálculo confiable→difusión); lectura rápida de muxes con dual-ADC+pipeline del settle (~717→~126→~50-60 µs); detección TEMPRANA con LUT de vecino-FÍSICO + vector de escape señalizado por `sensors_on_line` sin bit nuevo; calibración individual por sensor; criterio de "lectura confiable" + sellado a sentinela cuando `data_valid=0`; OTOS en paralelo (Nivel 0/1/2); RX por ISR del core + pizarra `sensor_slot.h`; plan F0-F7 + plan de banco. REUSA puro/host-testeado: `line_geometry`, `line_calib`, `sensor_health`, `lf_all_white`, `surface_monitor`, `down_tx`. NUEVO: ADC rápido + detección temprana + `sensor_slot.h` (no existe aún). NO reescribe `line_ring`/`dm_update`/`main_down` vivos) | — (diseño; el código vivo `line_ring.cpp` + cadena `dm_update`→`down_tx` siguen siendo lo que CORRE) |
| **CENTRAL — arquitectura del lazo RT de 4 capas (RX-ISR→pizarra→reflejos→control suave→FSM prolija)** | **`docs/firmware/ARQUITECTURA-LAZO-CENTRAL-RT.md`** (2026-06-14, diseño + plan de andamiaje gateado off-by-default: las 4 capas, el esqueleto del `loop()` nuevo, el patrón de FSM prolija con tabla de jugadas + `state_timer` uniforme, timeout-de-estado vs WDOG1, presupuesto de tiempo, plan por fases F0-F5 + plan de banco, 9 riesgos. Módulos PUROS a andamiar: `sensor_slot.h`/blackboard, `reflex.h`, `motor_slew.h`, `state_timer.h`, `gk_tuning.h`/`atk_tuning.h` — NINGUNO existe aún. NO reescribe `strategy.cpp` ni `main_central.cpp` vivos) | — (diseño; el código vivo `main_central.cpp::loop()` + `strategy.cpp` siguen siendo lo que CORRE). **2026-06-15: `motor_slew.h` (Capa 2) CABLEADO gateado** tras `-DCENTRAL_MOTOR_SLEW` (env `central_robot2_strafe_slew_bb`, reflejos bypasean la rampa) — default OFF, byte-idéntico. `state_timer`/`sensor_slot` PUROS host-testeados, NO cableados (son del rewrite de FSM/loop). Ver `journal/2026-06-15-integracion-rt-gateada.md`. **2026-06-16: `reflex.h` (Capa 1) CREADO PURO host-testeable** — máquina de prioridades STOP > BRAKE_EDGE (anti-latch 350 ms, fix María) > NONE, espejo exacto de `main_central.cpp:388-415`. 13 tests host (incluye regresión del deadlock 5 s de María). **NO cableado** (gate previsto `-DCENTRAL_REFLEX_LAYER` cuando se reescriba el loop post-Incheon). `gk_tuning.h`/`atk_tuning.h` siguen sin existir (extraer los ~80 constexpr de `strategy.cpp` con sus `#ifdef` build-time es riesgo P0 a 2 semanas de Incheon — explícito post-Incheon). Ver `journal/2026-06-16-central-rt-cierre-reflex-puro.md`. |
| **CENTRAL — estado RT auditado + plan de cierre para "placa operativa sin errores conocidos"** | **`docs/firmware/CENTRAL-RT-ESTADO-Y-CIERRE-2026-06-16.md`** (2026-06-16, 2 páginas: pág. 1 audita el `loop()` vivo verificado contra HEAD `8d284ae` —lo que YA es no-bloqueante (RX por ISR del core, drenado cooperativo de Serial1/Serial7, 0 `delay()` en `strategy.cpp`), lo que YA está cableado gateado (monitor USB DORMIDO, `motor_slew`, WDOG1, freno de borde con anti-latch), los disenos PUROS no cableados (`reflex.h` —desde 2026-06-16—, `state_timer.h`, `sensor_slot.h`), las mejoras esperadas y los 7 problemas verificados (P0: `CENTRAL_DEBUG_SERIAL` jitter; P1: TOP RX 64 B, `motors_brake` COAST, TASK-106 sin banco); pág. 2 = plan A-H ordenado: A banco T1-T7 monitor (TASK-106), B+C flips de competencia (cambian binario → banco primero), D titrar slew, E confirmar brake, F monitor-base 3a placa (✅ DONE 2026-06-15), G post-Incheon (reflex/seqlock/FSM prolija), H criterio de cierre. **Contratos TOP/DOWN intactos**. Honestidad: la mayoría de los items "errores conocidos" son bench-gated; Claude no puede cerrarlos solo. ) | — (el doc consolida estado + plan; los detalles arquitectónicos siguen en `ARQUITECTURA-LAZO-CENTRAL-RT.md` y los detalles del monitor en `PLAN-MONITOR-Y-CALIBRACION-CENTRAL.md`) |
| **CENTRAL — control de rumbo del arquero (lazo + latencias)** | **`docs/firmware/CONTROL-ARQUERO-LATERAL-Y-LATENCIAS.md`** (2026-06-14: lazo primario PID/PFM + frecuencias + lectura BNO 20→100 Hz + detección de línea + presupuesto de latencia. Diagnóstico de la "medialuna" = latencia dominante. Actuador = control continuo de potencia con zona muerta, NO bang-bang. 2 mejoras GATEADAS off-by-default: `TOP_BNO_FAST` (env `top_robot2_pri_fastbno`) + `GK_PFM_RATE_DAMP`/`heading_rate.h` (env `central_robot2_arquero_strafe_cam_ratedamp`) — pendientes de banco) | — |
| **TOP — capa de estimación/fusión (pose/pelota/arcos + velocidades)** | **`docs/firmware/ESTIMACION-FUSION-TOP.md`** (2026-06-14, diseño + backlog: la capa "estimador" entre la pizarra de sensores y el snapshot. Patrón predict@100Hz-OTOS + correct-con-ToF/cámara + gate de outlier. El heading es la raíz (interlock `imu_freeze`). 24 mejoras priorizadas; mucho YA EXISTE puro/host-testeado y SIN CABLEAR: `pose_fusion`, `pose_filter`, `imu_freeze`, `ball_sticky`, `otos_health`, `heading_rate`. Técnica: complementario 1D + α-β 1D, NO EKF. Review adversarial de 6 lentes. Todo gateado, banco post-Incheon) | — (diseño). **2026-06-15: CABLEADOS gateados** — `ball_sticky` (`-DTOP_CAM_STICKY`) e `imu_freeze` (`-DTOP_ENABLE_BNO_FREEZE_DETECT`) ya estaban; `pose_fusion`+`pose_filter` se cablearon en `build_snapshot` tras `-DTOP_ENABLE_POSE_FUSION` (env `top_robot2_pri_posefusion`) con **interlock `#error` que exige `imu_freeze`** + fallback exacto a localization si el ToF no ancla. Default OFF, byte-idéntico. **2026-06-15 (opt. TOP):** `imu_freeze` ganó la variante `imu_freeze_update_g` con **GUARDA DE GYRO** (arregla el falso-DEAD del robot quieto que apagó el flag el 2026-06-08; cableada en `sensors_imu.cpp`; envs `top_robot{1,2_pri}_bnofreeze`); módulo nuevo `pose_age.h` (edad fina del OTOS, nunca-recibido→MÁX) gatea `otos_fresh` a 60 ms. Banco: TASK-211 + TASK-210. **2026-06-15 (cierre estimador): 3 fixes NUMÉRICOS de `pose_fusion` (estaba cableado pero MAL al girar)** — H1 des-rotación del delta OTOS al marco de cancha vía módulo nuevo `src/shared/rot_lut.h` (sin/cos Q12 círculo completo); H2 gate de seed por consenso (3 ToF); H3 `heading_valid` (no anclar/corregir con heading muerto). Host-tested (`test_rot_lut`, `test_pose_fusion`); byte-idéntico (gateado off). Ver `journal/2026-06-15-cierre-top-estimador-correcto.md`. |
| **TOP — confiabilidad del heading: cross-validación de salud del BNO + centinela dual-BNO** | **Módulos puros `src/shared/imu_cross_validate.h` + `goal_rate_tracker.h`** (2026-06-15, host-tested 13+7; gateados/SIN cablear) + análisis `team-tasks/TASK-212` + banco `team-tasks/TASK-213`. Deciden la salud del primario por datos INDEPENDIENTES (mediana de OTOS+cámara+centinela), con anti-falso-veto (nunca MALO con <2 refs indep), consenso K-ventanas y scheduler del centinela @1Hz. **NO emiten failover** (rechazado: un heading plausible-pero-malo es peor que `heading_valid=0`). El modo CONGELADO lo cubre `imu_freeze.h::imu_freeze_update_g` (INC-1/Fase 0, ya cableado gateado). Journal: `journal/2026-06-15-fusion-confiabilidad-heading-bno.md`. | El árbitro auto-referencial de `imu_fusion.cpp` ("más estable") es engañable por un congelado — estos módulos lo SUPERVISAN (no lo reemplazan). El failover físico idx0→idx1 NO existe hoy en `imu_fusion` (es trabajo 2027). |
| **TOP — predicción de rumbo (extrapolación / predict step para datos stale)** | **Módulo puro `src/shared/heading_predict.h`** (2026-06-17, host-tested `test_heading_predict` 14/14; CABLEADO gateado `-DTOP_ENABLE_HEADING_PREDICT`, default OFF). En vez de transmitir el último heading "quieto" (zero-order hold) transmite `heading_crudo + ω·Δt` (ω MEDIDA del gyro, Δt = edad del fusionado) con CAP `max_extrap_ms`~60 ms (≪ `SNAPSHOT_TIMEOUT_MS`=500) + deadband + re-anclaje en muestra fresca. Cableado en `sensors_imu.cpp` (alimenta en el tick con `in[0].gyro_z_dps`) + los 2 sitios de TX (`main_top.cpp` build_snapshot y `snapshot_emitter.cpp` emisor) bajo el flag; el heading CRUDO queda intacto para `imu_freeze`/`localization`. Env `top_robot2_pri_hpredict`. Competencia byte-idéntica (flag OFF; verificado: `top_robot2_pri` FLASH data 102584 sin cambio, +384 B code solo con el flag). Doc: `research/completed/2026-06-17-extrapolacion-predict-step.md`. **Banco: TASK-218** (A/B medialuna del arquero). | NO extrapolar la pelota de ENTRADA (doble-conteo con el D del PID → la pelota ya usa `ball_predict`); NO extrapolar señales de seguridad (línea/obstáculo). La pose-XY ya tiene `pose_fusion` gateado aparte. La extrapolación NO reemplaza subir la tasa de sensado ni el fix del freeze del BNO (es aditiva). |
| **CENTRAL — guía de tuning (todos los parámetros tuneables, para cancha)** | **`docs/firmware/GUIA-DE-TUNING-CENTRAL.md`** (2026-06-15: TODOS los parámetros tuneables del firmware VIVO de la CENTRAL —potencias/PWM, tiempos, velocidades, ganancias, jugadas— organizados por "qué querés cambiar" (delantero/arquero/motores/PID/seguridad) + mapa flags→env. Cada uno con `archivo:línea`, valor actual, qué hace, rango seguro, efecto subir/bajar, voz de estudiante. **Verificado al 100% contra el código (~75 params, 0 discrepancias).** Topes de SEGURIDAD: `output_clamp` NUNCA >327 · `GK_EDGE_BRAKE_MAX_MS` NUNCA >500) | — (refleja el firmware vivo; re-verificar `archivo:línea` si se editan constantes) |
| **CENTRAL — plan de monitoreo + calibración desde la app (monitor-base)** | **`docs/firmware/PLAN-MONITOR-Y-CALIBRACION-CENTRAL.md`** (2026-06-15, workflow de 6 agentes, verificado `file:línea`: plan fasado para que la CENTRAL hable JSON Lines por USB —monitor DORMIDO estilo TOP, gateado `-DCENTRAL_USB_MONITOR`— y la app la muestre como 3ª placa. **Fase 1 IMPLEMENTADA 2026-06-15** —`telemetry_central.h` + `central_telemetry_serial` + parser/paneles `central` en monitor-base, contrato firmware↔app verificado end-to-end; **banco lo cierra el equipo**—. Fase 2 (calibrar params sin reflashear vía EEPROM+`SET` tipado) = backlog, usa la GUIA-DE-TUNING como catálogo. Reusa la caja negra existente) | Reemplaza el diseño de la `TASK-105` en 2 puntos (flag `CENTRAL_USB_MONITOR` ≠ `DEBUG_TELEMETRY`; vista DENTRO de monitor-base, no binario aparte) — la TASK-105 queda **backlog 2027** (decisión Gustavo), este doc es el plan vigente |
| **Behind-the-ball** | `src/shared/behind_ball.{h,cpp}` + 16 tests. Llamado por `strategy.cpp:230` (estado POSITION). Versión RELATIVA (sin pose absoluta). | El pseudocódigo de `FIRMWARE-PLACA-CENTRAL.md §8.4/§8.5` con `OPP_GOAL_X/Y` es Nivel 3 futuro (requiere EKF) — ya marcado en el doc |
| **Fusión cámaras (TOP)** | `src/shared/cameras_fusion.{h,cpp}` + `src/top/cameras_runtime.{h,cpp}` + 16 tests. Llamado por `main_top.cpp`. | — (sin doc rival) |
| **WorldSnapshot (struct compartido)** | `docs/firmware/CONTRATO-DATOS-CENTRAL.md` + `CONTRATO-DATOS-TOP.md §3` (**v3 = 31 B**: agrega `heading_valid` (flag bit4) + ángulo/distancia del arco propio; `static_assert(sizeof==31)` en `types.h`) | "v2 = 27 B" y "24 B" (mapa de flujo de `ARQUITECTURA-3-PLACAS-2026.md`) — **superados** por v3 (2026-06-03) |
| **DOWN — lectura de línea (la que CORRE)** | **Dos cadenas en paralelo (deuda viva, no archivar antes de Incheon):** (1) `src/down/line_ring.{h,cpp}` llamado por `main_down.cpp` para lectura cruda 1 kHz; (2) `src/down/down_model + line_geometry + line_tracker + line_calib + surface_monitor + down_encode` llamados por `comm_central.cpp` para armar `LineStatusV2` que va al CENTRAL. **Robustez (2026-05-29):** `dm_update` rechaza saturación "todo blanco" vía `lf_all_white()` y persiste/carga calib en EEPROM (`comm_central_load_persisted_calib`). | `docs/firmware/FIRMWARE-PLACA-ABAJO.md` describe solo (1) — desactualizado, banner agregado |
| **DOWN — pinout Teensy↔CD4051↔sensores** | **`hardware/electronics/2026-05-19-pinout-down-extraido-schematic.md`** + copia en el pack `down-board-pack/01-pinout-y-posiciones.md`. **VALIDADO EMPÍRICAMENTE 2026-05-24** en banco (Gustavo + Claude, sesión ejecución directa): pinout aplicado al firmware (config_down.h + line_ring.cpp), verdict del diag_capture = 0 muertos, los 32 sensores responden. La arquitectura "cada mux tiene 3 SEL propios" quedó confirmada. Cierre formal con multímetro (TASK-026) baja de P0 a P2. Ver journal `2026-05-24-hardware-up-down-anillo-linea.md`. | `src/down/config_down.h` ANTERIOR (`PIN_MUX_SEL_A=2,B=3,C=4,INH=5..8`, `PIN_MUX_OUT={A0,A1,A2,A3}`) — reemplazado. Sección DOWN de `mapa-pines-placas-nuevas.md` líneas 130-134 ("A/B/C compartidas") — SUPERADO. |
| **DOWN — pack autocontenido para programar (punto de entrada IA)** | **`hardware/electronics/down-board-pack/`** — todo lo necesario para entender, programar, testear y diagnosticar la placa DOWN en un solo lugar. 47 archivos, 2.4 MB. Contiene: 4 docs canónicos curados (`01-pinout` + `02-funcionalidad` + `03-contrato-datos` + `04-protocolo-comunicaciones`) + snapshot del firmware vivo (`firmware/down/` + módulos `firmware/shared/`) + 8 tests host-native + diag de hardware + scripts + ground-truth (SCH/PCB JSONs + PDF + BOM). El `README.md` tiene un índice "pregunta → doc". **Regla**: si el pack contradice al repo vivo, gana el repo vivo (es snapshot del 2026-05-24). | Otros docs que tocan DOWN tangencialmente (journals, plans superpowers, research in-progress, team-tasks) — quedan en sus carpetas originales como historia/gestión, no se duplican al pack. |
| **CENTRAL — pack autocontenido para programar (punto de entrada IA)** | **`hardware/electronics/central-board-pack/`** — equivalente al pack DOWN pero para la placa CENTRAL (Zircon Rev v15 + Teensy 4.1). 39 archivos. Contiene: 5 docs canónicos curados (`01-pinout-y-hardware` + `02-funcionalidad` 3 capas FSM/PIDs/motores + `03-contrato-datos` WorldSnapshot v2 + `04-protocolo-comunicaciones` + `05-arquitectura-3-placas`) + snapshot del firmware vivo de `src/central/` (14 archivos) + módulos `src/shared/` usados por CENTRAL (strategy_transitions, behind_ball, pids, kinematics, motion_target, proto, types) + 7 tests host-native (test_strategy_transitions 35 + test_behind_ball 16 + test_pids 17 + test_kinematics 11 + central_contract/trajectory/motion). Documenta los 2 robots (ROBOT1 arquero + ROBOT2 delantero) con sus pinouts distintos. **NO incluye ground-truth** (no hay schematic JSON del Zircon disponible, solo `Zircon.pdf` que queda fuera por binario pesado). **Conflicto pines 7/8 RESUELTO 2026-05-31**: se movieron los UART (DOWN→CENTRAL a Serial1 0/1, TOP→CENTRAL a Serial7 28/29); los pines 7/8 quedan solo para el driver del motor 2 (U17). | Igual que DOWN: journals, tasks, plans no se duplican al pack. `Zircon.pdf` queda fuera. `software/libraries/zirconLib/` queda fuera (legacy 2025). |
| **TOP — pack autocontenido para programar (punto de entrada IA)** | **`hardware/electronics/top-board-pack/`** — equivalente al pack DOWN/CENTRAL pero para la placa TOP (Teensy 4.0 master + PCB custom "Roboliga2026 TOP"). ~36 archivos. Contiene: 6 docs canónicos curados (`01-pinout-y-hardware` + `02-funcionalidad` + `03-contrato-datos-top` + `04-contrato-datos-camaras` + `05-protocolo-comunicaciones` + `06-arquitectura-3-placas`) + snapshot del firmware vivo de `src/top/` + módulos `src/shared/` + tests host-native + ground-truth. **Actualización 2026-05-30/31**: (1) los 4 ToF VL53L7CX ya NO son stub — enumeran en el bus `Wire` con LP por bodge (pines **{9,10,11,12}** → 0x2A..0x2D, confirmado en banco); el **bus `Wire2` (24/25)** del TOP quedó **liberado** (corrección 2026-06-09: los pines 24/25 del Teensy 4.0 son `Wire2`/LPI2C4, antes mal llamado "Wire1"; es el bus del 2º BNO de ROBOT2 — ver TASK-207). (2) **swap 2026-05-31 (TASK-204)**: la **cámara trasera quedó soldada en Serial5 (pin 21)** (confirmado en banco), así que el UART **TOP→CENTRAL se movió a Serial7 (28/29)** (`comm_central.cpp` → Serial7; `cameras_runtime.cpp` → trasera Serial5); además el HC-SR04 quedó en pines 4/3. (3) **pin 10 colisiona** entre LP ToF[1] y dipswitch de rol → reubicar. | Igual que DOWN/CENTRAL: journals, tasks, plans no se duplican al pack. `mapa-pines-placas-nuevas.md` y `2026-05-17-placa-top-analisis-gerbers.md` quedan en sus carpetas como historia. |
| **Convención de izquierda/derecha + ejes del robot (primera persona)** | **`docs/CONVENCION-EJES-ROBOT.md`** — CANÓNICA. +X=derecha, +Y=frente, izq/der en primera persona (parado EN el robot mirando al frente). Mapeo ToF (TOF2=derecha, TOF3=izquierda) + sentido de giro CW/CCW + temas a verificar en HW. | Comentarios contradictorios YA corregidos: `cameras_fusion.h` (decía +90=izq), `localization.h` (decía [2]=izq,[3]=der). Si algún archivo contradice el doc canónico, el archivo está mal. |
| **Convención de ejes de la cancha (X/Y/θ del robot)** | **`docs/CONVENCION-EJES-ROBOT.md` §2** (CANÓNICA, corregida 2026-06-03 contra el field spec oficial RCJ 2026) + `src/top/pinout_common.h` (`FIELD_HEIGHT_MM=2430` = eje **Y** arco-a-arco/largo; `FIELD_WIDTH_MM=1820` = eje **X** lateral/corto). +Y va al arco rival = lado LARGO. | `docs/superpowers/specs/2026-05-25-localization-sprint1-trilateration-design.md` §4.1 (decía +X=largo/+Y=corto — **INVERTIDO**, banner de corrección 2026-06-03). `docs/firmware/FIRMWARE-PLACA-ARRIBA.md` §6.4 (dimensiones X=1820/Y=2430 correctas, pero usa heading legacy θ=90°=+Y, no la convención canónica heading-0=+Y) |
| **Geometría física del robot (TOP) — offsets/alturas/orientación de sensores respecto del centro** | **`src/shared/robot_geometry.h`** (firmware, PURO host-testeado) + espejo **`tools/monitor-base/monitor_base/robot_geometry.py`** + doc **`docs/firmware/ROBOT-GEOMETRY.md`** (2026-06-16). Frame = CONVENCION-EJES (+X der, +Y frente, +heading CCW). ToF ~60 mm/h170, cámaras ~70 mm/h120, US ~70 mm; Ø~170. ⚠️ APROXIMADOS (medir). Math: `geom_rotate_offset` (offset robot→cancha) + `geom_center_dist_to_wall` (corrección sensor→centro). Tests: `test_robot_geometry` (host) + `tests/test_robot_geometry.py` (monitor). | **NO cableado aún:** la corrección offset→centro en `localization.cpp` (paso gateado + banco) y el RENDER a tamaño real (BLOQUEADO por conflicto de heading CCW-canónico vs `gui_field.robot_rel_to_field` CW — reconciliar antes). Fase B: proyección por-zona (rayos diagonales) con FOV/tilt medidos. |
| **TOF — procesamiento por zonas (rotación firmware-dueño + reductor robusto)** | **`src/shared/tof_zone_mask_orient.h`** (rota/espeja la máscara canónico→crudo, MISMA convención que `tools/monitor-base/.../tof_layout.zone_source_map`; host `test_tof_zone_mask_orient` 7/0) + **`tof_zone_masked_robust`** en **`src/shared/tof_zone_mask.h`** (descarta zonas > dimensión de cancha + outliers bajos = rebote en robot; host `test_tof_zone_robust` 8/0). Cableados en `src/top/sensors_tof.cpp` tras `-DTOP_ENABLE_TOF_ROT` / `-DTOP_ENABLE_TOF_ROBUST` (default OFF → byte-idéntico). Comandos `TOF n ROT/FLIP` (`telemetry_top` + `top_telemetry_serial`) → persisten en `TopConfig`/EEPROM. App: `tof_layout.FIRMWARE_OWNS_ROTATION` (default False=plegado histórico). Envs banco `top_robot2_pri_{tofrot,tofrobust,zonas}`. Diseño: `docs/superpowers/specs/2026-06-17-localizacion-tof-pose-xy-design.md`. ⚠️ banco lo cierra el equipo. | El promedio plano (`tof_zone_masked_mean`) sigue siendo el default (flags OFF). El mapeo zona→azimut para lidar sigue pendiente (`src/shared/tof_zone_orient.h:18-20`). |
| **CENTRAL — motores: índice↔driver↔pines + sentido + posición + ángulos + piso + MOTION LATERAL estándar** | `src/central/config_central.h` (pines + `MOTOR_INVERT` + `WHEEL_ANGLES_DEG` + `MOTOR_MIN_PWM[3]`, todos POR robot en el `#if`) + `src/central/motors_zircon.cpp` (impulso inicial + corte de trasera) + `docs/firmware/MOTION-CONTROL-PLAN-2026.md`. ROBOT1 **DEFINIDO en banco** (Gustavo 2026-06-08 + María/Elías 2026-06-01): posición física **M1=delantera-IZQUIERDA (U5 2/5/3) · M2=delantera-DERECHA (U17 8/7/6; INVERTIDO HW HASTA la reparación 2026-06-10/11 — el recableado lo dejó DERECHO) · M3=trasera (U7 11/12/4)**; los 3 giran HORARIO desde el centro → `MOTOR_INVERT={+1,+1,+1}` (banco piso 2026-06-11, commit 8d5fc90), `WHEEL_ANGLES_DEG={330,210,90}`. **ROBOT2 VALIDADO en banco (Gustavo 2026-06-09)**: pines **IGUALES a R1 (NO rotados)** y `MOTOR_INVERT={+1,+1,+1}` (el U17 de ESA placa no está invertido por HW). **TÉCNICAS ESTÁNDAR de movimiento LATERAL (decisión Gustavo 2026-06-09 — se usan SIEMPRE que el robot se mueva lateral, en TODOS los programas; validadas banco R2 `diag_central_strafe_robot2_kick`, "anda bien"):** (1) **piso de PWM por rueda** `MOTOR_MIN_PWM[3]={70,70,107}` (delanteras oblicuas 70 · trasera alineada 107; barrido de banco 42→50→70→85→95→100→105→107); (2) **impulso inicial fijo por rueda** `{145,145,150}` PWM ×40 ms en la transición parado→comando (detrás de `-DCENTRAL_MOTOR_KICKSTART`; **subido de `{130,130,140}` a `{145,145,150}` por Elías 2026-06-14 — el CÓDIGO `motors_zircon.cpp` manda**; la trasera es la más alta porque "se quedaba"); (3) **freno anticipado de la trasera**: `motors_set_rear_cut()` corta la trasera (idx2) a 0 en los últimos 66 ms del tramo mientras las delanteras terminan (gateado `-DCENTRAL_REAR_BRAKE_LEAD`, tunable `-DDIAG_STRAFE_REAR_LEAD_MS`; **HOY cableado solo en `diag_central_strafe.cpp`** — llevarlo a la FSM del arquero es tema-a-analizar: el corte necesita saber cuándo TERMINA el movimiento y la FSM no tiene ese evento). Física aprendida: el PWM **NO es proporcional a la velocidad y es DISTINTO por rueda** — en el strafe la trasera debe girar al DOBLE (cinemática: fronts 0.5·vx, rear 1.0·vx) pero por ir ALINEADA (menos fricción que las oblicuas) lo logra con ~1.5× el PWM (107 vs 70), no 2×. **R1: pisos {70,70,107} + kickstart {130,130,140} VALIDADOS en piso 2026-06-11.** | `{+1,-1,+1}` **para R1** — **superado 2026-06-11** (compensaba el cableado PRE-reparación del U17; si se recablea como estaba, volver al `-1`). `WHEEL_ANGLES_DEG={60,-60,180}` ("TENTATIVO / da círculos") — **superado** por `{330,210,90}` (2026-06-08: el viejo estaba en eje +Y y la fórmula usa +X). Piso único escalar `MOTOR_MIN_PWM=N` y `MOTOR_GAIN` — **superados** por el array por rueda. **`MOTOR_MIN_PWM[3]={70,70,42}` como valor FINAL de R1 — superado 2026-06-09**: el 42 de la trasera salió del banco R1 del 2026-06-08 (se bajó porque con piso alto el robot ROTABA en el strafe); esa historia se preserva en el comentario de `config_central.h` — **si R1 rota con 107, bajar idx2 gradualmente**. "ROBOT2 sin validar (pines rotados)" — **superado** (banco 2026-06-09: NO rotados). |
| **CENTRAL — control de movimiento de bajo nivel (impulso/freno/rampas/PWM por movimiento)** | `docs/firmware/MOTION-CONTROL-ACTUAL.md` (estrategia 2026: cinemática + piso por rueda; GAP honesto vs histórico con P1/P2) + `docs/firmware/MOTION-CONTROL-HISTORICO.md` (valores reales 2025: impulso 150×70 ms para giro, freno anticipado de la trasera 60 ms auto-calibrado por gyro, plugging M1=250/M2=170 ×200–300 ms). Auto-generados del análisis de `software/_deprecated-2025/` + `software/staging/shared/test-*` (2026-06-08). **+ `docs/firmware/MOTION-CONTROL-PLAN-2026.md`** (plan de portado en 4 capas, GATEADO) + módulos PUROS host-testeados `src/shared/{gk_motion_speed,motor_kickstart,motor_brake}.{h,cpp}` (Capas 1-2; gateados, default OFF → binario de competencia idéntico; Capa 3 cascada pendiente). | El firmware 2026 HOY solo tiene piso de deadzone por rueda — NO porta impulso/freno-reversa/rampas (módulos escritos pero NO cableados; pendiente de banco). `software/_deprecated-2025/` y `software/staging/` están CONGELADOS (no flashear). |
| **Programas 2025 (Nacional BsAs) — transcripción FIEL para recrear el comportamiento EXACTO** | **`docs/internal/ANALISIS-FIEL-ARQUERO-2025.md` + `ANALISIS-FIEL-DELANTERO-2025.md` + `ANALISIS-FIEL-ZIRCONLIB-2025.md`** (2026-06-18, workflow 14 agentes + crítico de completitud que re-leyó el programa: TODA la lógica de juego / PWM / tiempos / timeouts / umbrales de `definitivo-arquero` / `definitivo-delantero` / `zirconLib`, línea por línea, por ROBOT1/ROBOT2; tablas exhaustivas 153/152 ítems; arquero `cobertura_ok=TRUE`, delantero fiel + 4 ítems no materiales en apéndice). **Hallazgo:** ambos `definitivo-*` son el MISMO firmware unificado arquero+delantero (cambia `#define ROBOT1/2` + estado inicial). | Complementa (NO reemplaza) los `docs/internal/analisis-definitivo-{arquero,delantero}.md` (que son análisis de bugs/refactor, no transcripción fiel). El código fuente sigue CONGELADO en `software/_deprecated-2025/`. |
| **DOWN — contrato de datos** | `docs/firmware/CONTRATO-DATOS-DOWN.md` (LineStatusV2) | — |
| **TOP — contrato de datos** | `docs/firmware/CONTRATO-DATOS-TOP.md` | — |
| **TOP — telemetría/monitoreo USB (frame v2 + monitor dormido + comandos config)** | `software/teensy/Soccer 2026/docs/firmware/TELEMETRIA-TOP.md` (frame v2: per-cámara `camf`/`camb` + base `base`/`line` OTOS+línea+escape; monitor DORMIDO en competencia; comandos A2.1 `CAM/BNO/US/TOF/CFG`) + módulo puro `src/shared/telemetry_top.{h,cpp}` | — |
| **TOP — config persistente de sensores (EEPROM): deshabilitar cámara/BNO/ToF/US + ubicación ToF** | `src/shared/top_config.{h,cpp}` (PURO: struct + serialize/CRC + defaults no-op) + `src/top/top_eeprom_config.{h,cpp}` (glue, offset **368**) + `docs/firmware/EEPROM-MAP.md` (mapa por placa). ⚠️ El comentario "A2.2 (zonas/rotación ToF) reservado, sin apply aún" quedó **STALE**: el código YA aplica `zone_mask` (en vivo, cada tick) y `rotation/flip` (con `-DTOP_ENABLE_TOF_ROT`) — ver fila de abajo. | — |
| **TOP — ToF: bloqueo de zonas / rotación / EEPROM, y por qué el MONITOR no coincidía con la TOP (ANÁLISIS)** | **`docs/firmware/TOF-ZONAS-TOP-MONITOR-ANALISIS.md`** (2026-06-21, workflow 7 lectores + verificación adversarial): cómo lee la TOP los 4 VL53L7CX (round-robin, 4x4=16 zonas, status {5,6,9}), la máscara `zone_mask` (en vivo) vs rotación/flip (boot, con flag), persistencia EEPROM (blob 93B, offset 368, CRC16), promedios/outliers (3 reductores), y las **10 divergencias TOP↔monitor verificadas** (la #1: la telemetría manda zonas CRUDAS sin máscara → el monitor pinta su `.json`, NO la EEPROM; no hay readback). Incluye P0/P1/P2 para habilitar. | — (análisis; el código sigue siendo la fuente: `sensors_tof.cpp`/`tof_zone_mask*`/`top_config*`) |
| **TOP — cámaras (protocolo)** | `docs/firmware/CONTRATO-DATOS-CAMARAS.md` | — |
| **Cámaras (OpenMV) — script que va EN la cámara (PRODUCCIÓN)** | ⚠️ **CORREGIDO 2026-07-23: `camaras-openmv/main.py` NO EXISTE.** Los archivos reales son **4, uno por cámara**: `hardware/electronics/camaras-openmv/main-r1-frontal.py`, `main-r1-trasera.py`, `main-r2-frontal.py`, `main-r2-trasera.py` (rename de Elías 2026-06-21, commits `c4510b9`/`a888217`/`2f19b2c`). Cada uno se copia como `main.py` **en su cámara**. Detección que ANDA (LED indicador + colores LAB calibrados + recorte inferior ROI + homografía de Elías @VGA) + comunicación **v2 con CRC8+END**. README al lado; `main-comunicacion-vieja.py` = referencia v1 (NO flashear). | ⛔ `cameraFront-pack/.../cam-frontal-n6.py` y `cameraBack-pack/.../cam-trasera-n6.py` — reescritura SIN LED/colores/recorte → NO detecta → **DEPRECADOS** (banner 2026-06-08). NO flashear esos. |
| **Cámaras (OpenMV N6 / PAG7936) — LÍMITES FÍSICOS del sensor + API de firmware 5.0.0** | **`docs/firmware/PAG7936-LIMITES-Y-API-FW5.md`** (2026-07-23, medido en placa + verificado contra el fuente v5.0.0). Los 3 techos: ganancia **24,08 dB** (16×), exposición **8.248 µs** a 120 fps, y `rgb_gain_db` **se ignora entero** (`pag7936.c:806-809`). **Todos los clamps son SILENCIOSOS.** Cómo subir el techo de exposición con `set_framerate()` + sus 3 trampas. Migración de API a fw 5.0.0 (blobs y `get_statistics()` son attrtuples: `b.cx` no `b.cx()`; `draw_line` pide tupla) con el shim `_bv()` compatible 4.8.1↔5.0.0. **Regla nueva: un `set_auto_*` sin su `get_*` impreso al lado no se commitea.** | Cualquier doc que dé por buenos `exposure_us=100328` o `rgb_gain_db=(...)` como calibración — **nunca se aplicaron**. Ver `research/completed/2026-07-23-rootcause-linea-tof-camaras-post-incheon.md`. |
| **Cámaras (OpenMV N6) — calibración de COLOR (LAB/exposición)** | `docs/firmware/CALIBRACION-VISION-N6.md` (+ kit `calib-lab-n6.py`) | ⚠️ Sus thresholds LAB "finales" **no son los que corren** (el código vivo manda). Su parte de exposición está **superada** por `PAG7936-LIMITES-Y-API-FW5.md`. |
| **Por qué fallaron línea/piso, ToF y cámaras en Incheon (ROOT-CAUSE)** | **`research/completed/2026-07-23-rootcause-linea-tof-camaras-post-incheon.md`** (auditoría de 21 agentes, 98 hallazgos, verificado `archivo:línea`). Línea: el lazo adaptativo del carpet es un **integrador cuantizado y biestable** que barre el margen en 0,4-1,7 s (`line_calib.cpp:41-42`, `alpha=0.02` a 200 Hz) — **hay histéresis**, el problema es que el umbral se mueve debajo de ella. ToF: alcance real ~1,4 m contra pared negra vs cancha de 2.430 mm, **+ jugaron con el env sin `-DTOP_TOF_MAXRANGE`**, + bug confirmado: `sensors_tof_get_min_distance_mm()` **ignora `enabled`** (`sensors_tof.cpp:697-714`). Cámaras: las 4 comparten **la misma homografía**, la de la frontal del R1. | — (análisis; el código sigue siendo la fuente) |
| **Detección de color (pelota+arcos) + selección de arco — EXPLICACIÓN de cómo funciona (overview)** | **`docs/firmware/DETECCION-DE-COLOR-COMO-FUNCIONA.md`** (2026-06-21, explicativo, voz de estudiante): recorrido cámara→TOP→CENTRAL, mecanismo `find_blobs`+LAB, mensaje 201/202/203, y `goal_polarity` (rol propio/rival por ángulo+latch). Consolida y APUNTA a las fuentes canónicas. | — (overview; las fuentes OPERATIVAS siguen siendo `camaras-openmv/main.py` + `CALIBRACION-VISION-N6.md` + `goal_polarity.{h,cpp}` — este doc NO las reemplaza) |
| **Cámaras (OpenMV N6) — calibración de HOMOGRAFÍA/XY** | `docs/firmware/CALIBRACION-HOMOGRAFIA-XY-N6.md` (lona con grilla + `calib-homografia-n6.py` en cámara + `vision-optimization-pack/tools/solve_homografia.py` en PC). **2026-06-15:** el solver acepta `--csv FILE` + `--validate`, valida puntos degenerados (colineales/<4/H[2,2]≈0) y la H se pega en **`camaras-openmv/main.py`** (producción), NO en `cam-*-n6.py` (deprecados). Test `tools/test_solve_homografia.py` 14/14. Ver `journal/2026-06-15-task022-tooling-solver-homografia.md`. | El "Paso 4 — Homografía" (stub de 4 puntos a ojo) de `CALIBRACION-VISION-N6.md` y el §3 de los `04-calibracion-lab-y-homografia.md` de los packs — **superados** (ahora apuntan a este doc). |
| **Cámara FRONTAL (OpenMV) — pack autocontenido** | **`hardware/electronics/cameraFront-pack/`** — pack equivalente a los de placas pero para la cámara OpenMV frontal. 16 archivos, 152 KB. Contiene 4 docs curados (`01-hardware-y-conexion` + `02-funcionalidad` + `03-protocolo-comunicacion` + `04-calibracion-lab-y-homografia`) + snapshot del firmware OpenMV genérico actual + **template objetivo `target-cam-frontal-template.py` con bugs P0 corregidos** + parser del lado Teensy + tests de fusión. Para reemplazar el script genérico actual (`software/vision/enviar coordenadas 2 arcos y pelota`) por uno específico calibrado para esta cámara (TASK-022). | Pack hermano: `cameraBack-pack/`. El skill `openmv-vision-tuning` cubre el workflow general (no se duplica en el pack). |
| **Cámara TRASERA (OpenMV) — pack autocontenido** | **`hardware/electronics/cameraBack-pack/`** — pack equivalente al frontal pero para la cámara trasera. 16 archivos, 156 KB. Misma estructura, con énfasis especial en la **rotación 180°** que el TOP aplica a sus coordenadas (`cameras_fusion.cpp:25-29` con `cam_id=1`). Documenta por qué la cámara trasera NO debe rotar sus coordenadas internamente — la rotación es responsabilidad del TOP. Diferencia clave con la frontal: distinto Serial del Teensy (**Serial5** vs Serial3 — la trasera quedó soldada en Serial5, confirmado en banco 2026-05-31; ver fila TOP), distinto conector (U9 vs U8), distinto cam_id (1 vs 0), H_MATRIX independiente, HMIRROR/VFLIP probablemente distintos por montaje espejado. | Pack hermano: `cameraFront-pack/`. |
| **Comunicaciones entre placas (diseño)** | `docs/decisions/2026-05-18-diseno-comunicaciones-robusto-definitivo.md` (declara fuente única, reemplaza al protocolo-comunicaciones-entre-placas.md del mismo día) | `docs/decisions/2026-05-18-protocolo-comunicaciones-entre-placas.md` (base conceptual superada). `docs/decisions/2026-05-18-verificacion-protocolo-comunicaciones.md` (insumo de la verificación, no decisión) |
| **Comunicación inter-robot (SuperTeam)** | `docs/decisions/2026-05-17-comunicacion-inter-robot-superteam.md` (decisión VIGENTE: NO implementar para Incheon, Opción A). La **arquitectura concreta de la Opción B** (gateway ESP32-S3 en el bus CAN, gate de modo árbitro) vive en `docs/decisions/2026-06-03-bus-can-general-y-flasheo-por-can.md` §7-bis (forward-looking, NO cambia la decisión) | — |
| **Bus general CAN + flasheo por CAN (PROPUESTA, post-Incheon)** | `docs/decisions/2026-06-03-bus-can-general-y-flasheo-por-can.md` — propuesta de migrar el transporte inter-placa de UART punto-a-punto a un **bus CAN único** (3 Teensy + 4 OpenMV + bridge ESP32-S3, transceiver SN65HVD230, JST-GH) y de **flashear firmware por el bus** (FlasherX, integridad CRC en 3 capas, gate de mantenimiento). **§7-bis** analiza la extensión inter-robot (SuperTeam) por el mismo gateway. **Apéndice A**: esquema de interfaz CAN por Teensy (SN65HVD230) + pines CAN1/2/3 + BOM/costo + sketch PoC Fase 0. **Status: propuesta, NO validada en banco.** | **NO reemplaza** el diseño VIVO de comunicaciones UART (`2026-05-18-diseno-comunicaciones-robusto-definitivo.md`), que sigue siendo el transporte canónico que CORRE. Las **capas fail-safe 0–6** de ese doc se **preservan**; CAN solo cambia las capas 1 (física) y 2 (framing). |
| **Firmware placa COMM (ESP32-C6) — flash** | `hardware/electronics/comm-board/2026-05-17-procedimiento-flash-firmware-c6.md` + `2026-05-17-placa-comm-componentes-y-circuito.md` (branch `esp32-c6`, pinout 6/7/18/9/20/19, BLE `RCJs-m_<MAC>`) | `journal/2026-05-15-firmware-comm-c6-flash-procedure.md` (branch `master`/C5, pinout 2/3/10/7/9/8 — INCORRECTO, ya tiene banner) |
| **Setup entorno PlatformIO + Avast** | `docs/firmware/SETUP-ENTORNO-BUILD-WINDOWS.md` (con callout Avast del 18-may) + `team-tasks/TASK-025` (excepción Avast por máquina) | — |
| **PIDs (heading, lateral, distancia)** | `src/shared/pids.{h,cpp}` + 17 tests. Llamado desde `strategy.cpp`. | — |
| **Kinemática inversa omni-3** | `src/shared/kinematics.{h,cpp}` + 11 tests. Llamado desde `motors_zircon.cpp`. | — |
| **Tests host-native — qué son, cuántos de cada tipo, qué errores atajaron (explicación para jurados/equipo)** | **`docs/competencia/TESTS-HOST-NATIVE-EXPLICADOS.md`** (versión larga: las 61 suites mapeadas por subsistema con conteo real, las 7 categorías de chequeo, los bugs reales que la suite guarda contra regresión, y el límite honesto host≠banco; cifra viva medida con `scripts/run-host-tests.sh`) | TDP §3.1-3.3 es el RESUMEN del mismo material (no rival: el doc es la expansión). Los conteos por módulo sueltos en esta tabla ("17 tests", "11 tests") son históricos — el conteo vivo por suite está en el doc nuevo |
| **CENTRAL — diag de motores en banco** | [`docs/firmware/DIAG-CENTRAL-MOTORS.md`](firmware/DIAG-CENTRAL-MOTORS.md) + sketch [`src/diag/diag_central_motors.cpp`](../software/teensy/Soccer%202026/src/diag/diag_central_motors.cpp) + env `[env:diag_central_motors]` en `platformio.ini`. Test individual de los 3 H-bridges del Zircon Rev v15 con onda PWM controlada por botón (pin 9). Agnóstico al rol — usa pines hardcodeados, no `config_central.h`. Sirve para mapear "motor N firmware → rueda física". (El conflicto 7/8 ya se resolvió 2026-05-31 moviendo los UART a Serial1/Serial7; los pines 7/8 quedaron para el motor 2.) | — |
| **CENTRAL — patrulla del arquero gatillada por el árbitro** | [`docs/firmware/DIAG-CENTRAL-ARBITRO-STRAFE.md`](firmware/DIAG-CENTRAL-ARBITRO-STRAFE.md) + sketch [`src/diag/diag_central_arbitro_strafe.cpp`](../software/teensy/Soccer%202026/src/diag/diag_central_arbitro_strafe.cpp) + envs `[env:diag_central_arbitro_strafe_robot1/2]`. Espera el START del árbitro (`world_model_match_running()` del WorldSnapshot del TOP, Serial7), patrulla lateral 30 cm IZQ→pausa 3 s→DER→pausa 3 s, STOP→frena, START→retoma. Override de banco por Serial (`s`/`x`) para probar sin COMM/TOP. OPEN-LOOP + mismos caveats de cinemática que el strafe por botón. | — |
| **CENTRAL — verificar recepción completa de TOP + DOWN** | [`docs/firmware/DIAG-CENTRAL-RX-ALL.md`](firmware/DIAG-CENTRAL-RX-ALL.md) + sketch [`src/diag/diag_central_rx_all.cpp`](../software/teensy/Soccer%202026/src/diag/diag_central_rx_all.cpp) + env `[env:diag_central_rx_all]`. Escucha DOWN (Serial1/pin 0) **y** TOP (Serial7/pin 28), decodifica los 4 mensajes (LineStatusV2 0x10 + Pose2D 0x11 + Velocity2D 0x12 de DOWN; WorldSnapshot 0x60 de TOP) campo por campo, y da un **veredicto de completitud** (`[OK]/[STALE]/[FALTA]` + Hz por mensaje + PASS/FAIL global + LED). Responde "¿la CENTRAL recibe TODO de TOP+DOWN?". (Para ver SOLO la línea de DOWN con más detalle: `DIAG-CENTRAL-COMM-DOWN.md`.) | — |
| **CENTRAL — diag de avance recto con PID heading** | [`docs/firmware/DIAG-CENTRAL-DRIVE.md`](firmware/DIAG-CENTRAL-DRIVE.md) + sketch [`src/diag/diag_central_drive_straight.cpp`](../software/teensy/Soccer%202026/src/diag/diag_central_drive_straight.cpp) + envs `[env:diag_central_drive_robot1]` y `[env:diag_central_drive_robot2]`. Valida end-to-end la cadena `WorldSnapshot (Serial1 TOP) → world_model → HeadingPID → kinematics inversa omni-3 → motors_zircon`. Botón pin 9 controla: FORWARD 3s → PAUSED 1s → REVERSE 3s → DONE. Sirve para tunear ganancias del HeadingPID y confirmar cinemática del robot armado. **NO hace PID diferencial OTOS** (CENTRAL no recibe las 2 lecturas crudas; requiere contrato v3 o bypass DOWN→CENTRAL, ambos post-Incheon). | — |
| **Simulación — entornos para probar el firmware SIN robot (alternativas y viabilidad)** | [`research/completed/2026-06-11-alternativas-entorno-simulacion-firmware.md`](../research/completed/2026-06-11-alternativas-entorno-simulacion-firmware.md) — informe 2026-06-11: ruta incremental recomendada (A) gemelo 2D host-native de la CENTRAL → (B) HIL CENTRAL real + fake TOP/DOWN por USB-UART → (D) SIL 3 placas lógicas con UARTs virtuales; Webots/rcj-soccersim = inversión 2027 (sinergia Erebus); Gazebo/MATLAB/CoppeliaSim/Renode descartados con motivo. Incluye plan de validación obligatorio (replay de caja negra). **Status: informe; NO hay simulador implementado todavía.** | — |

| **Localización / pose — CÓMO PENSARLA (qué técnica, por qué) + CÓMO CONSTRUIR el estimador** | **Skills `.claude/skills/localizacion-rcj-soccer/` + `fusion-pose-odometria-landmarks/`** (2026-06-14). La 1ª es la LENTE: explica odometría/VO, Visual SLAM, landmarks/trilateración, MCL/partículas, EKF, pose estimation con veredicto de factibilidad para OpenMV+Teensy y los mapea a los módulos reales; veredicto clave = la cancha es mapa conocido → NO SLAM, sí landmarks+fusión. La 2ª es el CÓMO: patrón predict/correct, `pose_fusion`/`pose_filter` ya escritos y NO cableados, protocolo de medir ruido antes de tunear. Son metodología/lente (como `control-pid-zona-muerta`), NO el código. | El código/implementación canónica NO cambia: `src/shared/localization.{h,cpp}` (trilateración VIVA) + spec `docs/superpowers/specs/2026-05-25-localization-sprint1-trilateration-design.md` + frame `docs/CONVENCION-EJES-ROBOT.md` siguen siendo la fuente de la IMPLEMENTACIÓN. Las skills apuntan a ellos, no los reemplazan. |
| **ToF VL53L7CX — lectura/calidad/calibración/POSICIONAMIENTO por paredes (cómo pensarlo)** | **Skill `.claude/skills/tof-vl53l7cx-lectura-calibracion-posicionamiento/`** (2026-06-21). Lente del sensor multizona: resolución 4x4/8x8, modo continuo vs autónomo, filtro por `target_status`, recorte de payload (`VL53L7CX_DISABLE_*`), coexistencia BNO+ToF en el bus (el band-aid 100 kHz), carga del firmware ~84 KB, calibración xtalk/offset, y de la matriz a la pose por paredes. COMPLEMENTA (no duplica) `localizacion-rcj-soccer` (elegir técnica) y `fusion-pose-odometria-landmarks` (cablear el filtro). | El código/implementación NO cambia: `src/top/sensors_tof.cpp`, `src/shared/localization.{h,cpp}`, `tof_zone_mask{,_orient}.h` siguen siendo la fuente VIVA. La skill marca 2 inconsistencias doc verificadas: `CONTRATO-DATOS-TOP.md` describe los ToF como "stub" (obsoleto) y `CONVENCION-EJES-ROBOT.md:153,158` dice grilla "8×8" (el código usa 4x4=16). |
| **Cinemática omni-3 a 120° — la matriz, signos, ω·R, saturación (la TEORÍA)** | **Skill `.claude/skills/cinematica-omni-3-120/`** (2026-06-21). La matemática/teoría del omni-3: la fórmula `v_i=-vx·sinθ+vy·cosθ+ω·R`, la matriz con `{330,210,90}`, el `+180` que corrige traslación, la trampa `OMEGA_SIGN` (el +180 NO toca la rotación → realimentación positiva), saturación por escalado. COMPLEMENTA (no duplica) `dinamica-omni-3-ruedas` (la planta MEDIDA: pisos PWM, deriva) y `control-pid-zona-muerta` (el lazo). | El código/implementación NO cambia: `src/shared/kinematics.{h,cpp}` + `src/central/{config_central.h,motors_zircon.cpp}` son la fuente VIVA. La skill ancla todo a esos `archivo:línea` y remite a la planta/lazo para tuning. |
| **Cámaras OpenMV N6 — visión por color, contrato cámara→TOP, calibración (lo OPERATIVO)** | **Skill `.claude/skills/openmv-n6-camara-vision-robocup/`** (2026-06-21). N6-específica y anclada al código: pipeline `find_blobs` LAB en CPU (lo que CORRE, sin NPU), bloqueo de exposición/WB, homografía atada a VGA, protocolo v2 11 bytes/CRC8 (`cameras.h`), fusión front+back + rotación 180° trasera, árbol de diagnóstico, y veredicto ML/NPU (2027, no Incheon). **REEMPLAZA a `openmv-vision-tuning`** como fuente operativa. | ⚠️ **`.claude/skills/openmv-vision-tuning/` quedó SUPERADA** (dice "OpenMV H7", pelota IR pasiva, arcos cyan/magenta — el hardware real es N6, pelota naranja por color, arcos amarillo/azul; banner agregado 2026-06-21). El código VIVO (`cameras.{h,cpp}`, `cameras_runtime.cpp`, `cameras_fusion.{h,cpp}`, `camaras-openmv/main.py`) no cambia. Inconsistencias marcadas: 3 sets de thresholds LAB divergentes y `h=95.0` vs `18.7` — cerrar en banco. |

## Deudas conocidas (no se resuelven antes de Incheon)

1. **DOWN lectura dual** (`line_ring` vivo + cadena nueva `down_model` viva en
   paralelo, posiblemente leyendo sensores 2 veces). Funciona ambos por
   separado. Decisión binaria pendiente post-Incheon: integrar todo en la
   cadena nueva o documentar formalmente la separación. → revisar en
   journal post-Nacional (noviembre 2026).
2. **Conectar `strategy.cpp` → `strategy_transitions`** (cerrar la red de
   testing de la FSM viva). Diseño en `journal/2026-05-15-analisis-firmware-y-fsm-testeable.md`
   "Conectar strategy.cpp" — pendiente ejecutar en post-Incheon (riesgo:
   tocar el cerebro).
3. ~~**`cameras_runtime` no llena `ball_vx/vy`**~~ → **RESUELTO (2026-06-03).** El
   módulo puro `ball_velocity` deriva la velocidad de la pelota y `cameras_runtime`
   la cablea (`cameras_get_ball_vx/vy_mm_s`); `build_snapshot` la mete al snapshot.
   El arquero ya la usa para anticipar (`ball_predict`). Fallback exacto con pelota quieta.
4. **TOP — ToF: ✅ RESUELTO por bodge de Enzo (2026-05-30).** El forense del
   2026-05-25 había concluido que los XSHUT/LP de los 4 ToF NO estaban ruteados
   en TOP rev 1.0 (máximo 2 ToFs sin rework). **Eso quedó SUPERADO**: Enzo
   cableó por bodge la pata LP de cada ToF a un pin del Teensy y movió los **4
   ToF al bus `Wire` (18/19)**. Banco 2026-05-30 (`diag_top_tof_census`, tras
   power-cycle): los **4 LP funcionan** → pines **{9,10,11,12} activo-alto**,
   enumeran a **0x2A/0x2B/0x2C/0x2D**. Esto liberó el bus `Wire2` (24/25) del TOP
   (corrección 2026-06-09: el bus de 24/25 es `Wire2`/LPI2C4, no `Wire1`; ahí va el BNO
   PRIMARIO, solo, sin ToF — TASK-207. **Arquitectura BNO canónica (corrección 2026-06-15):
   AMBOS robots, 2 BNO @ 0x28 en buses separados — primario Wire2 / secundario Wire; NO hay
   ningún BNO en 0x29. Fuente: `src/top/sensors_imu.h` + `src/top/pinout_common.h`.**
   **⚠️ HW Incheon (2026-06-17): config confirmada — AMBOS robots con 2× BNO funcionando (R1 vuelve a jugar CON gyro). Odometría OTOS es SOLO R1 (R2 no tiene). Banner canónico de config de sensores por robot: `docs/ESTADO-ACTUAL.md` (2026-06-17).**)
   **✅ Heading de R1 VALIDADO en banco (2026-06-21):** el "heading=0.0" previo era el flag `bno_left_en=0` en EEPROM (no el chip, no los ToF), ya fijado en `sensors_imu.cpp:284`; con `top_robot1_pri_rt` (ToF rangeando) el `hdg` sigue el giro. La hipótesis "los ToF congelan el BNO" (TASK-223) quedó como pista falsa para el primario (vive en `Wire2`, aparte). Fuente: `journal/2026-06-21-bno-heading-fix-config-flag-no-era-tof.md`.
   y habilita localización 2D por trilateración. Pines/direcciones reales
   ya en `pinout_robot1.h`/`pinout_robot2.h` (`PIN_TOF_XSHUT={9,10,11,12}`,
   `NUM_TOF_ACTIVE=4`). **Lección**: las direcciones I²C de los VL53L7CX
   persisten con 3V3 → power-cycle obligatorio al enumerar (no reset).
   Pendiente (firmware, NO bloqueante de hardware): confirmar ranging con
   `diag_top_tof_quad_live` + mapear posición física + extender `sensors_tof.cpp`
   (HAL Sprint B). Conflicto colateral: pin 10 = LP ToF y dipswitch de rol a la
   vez → reubicar rol. Ver `journal/2026-05-30-top-tof-4-en-bus-unico-enumeracion-ok.md`.
   (Plan de escalado: 6 ToF = 4 fijos + 2 móviles para pelota, ver `pinout_common.h`.)

## Mapa de docs superados (con banner)

Estos docs tienen banner explícito que apunta acá:

- `journal/2026-05-15-firmware-comm-c6-flash-procedure.md` → ver procedure del 17-may
- `docs/firmware/FIRMWARE-PLACA-ABAJO.md` → ver `CONTRATO-DATOS-DOWN.md` + this file
- `docs/ARQUITECTURA-3-PLACAS-2026.md` Mapa de flujo → ver `CONTRATO-DATOS-CENTRAL.md` para tamaño real de WorldSnapshot
- `docs/superpowers/specs/2026-05-18-central-strategy-core-design.md` → módulo archivado en `_archive/`, ver este archivo

## Cómo mantener esta tabla viva

- Cualquier PR que cree un doc nuevo de tema técnico → agregar fila acá.
- Cualquier PR que supere un doc existente → marcar superado acá + banner en el doc viejo.
- Sesión Claude que toca el repo: primera lectura obligatoria + `docs/ESTADO-ACTUAL.md`.
